[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "market-workstation"
version = "0.1.0"
description = "Local personal stock analysis and monitoring system"
requires-python = ">=3.12,<3.13"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "sqlalchemy",
  "alembic",
  "psycopg[binary]",
  "pydantic",
  "pydantic-settings",
  "python-dotenv",
  "httpx",
  "pandas",
  "apscheduler",
  "pytest",
  "pytest-asyncio",
  "ruff",
  "mypy"
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.setuptools.packages.find]
include = ["apps*", "config*", "services*", "workers*"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
exclude = '(^|/)(data/postgres|\.mypy_cache|\.pytest_cache|\.ruff_cache)(/|$)'
